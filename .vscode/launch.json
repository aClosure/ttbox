{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB (WSL)",
            "type": "lldb",                // 改了这里
            "request": "launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build Current C File"  // 方案二的自动编译依然有效
        }
    ]
}